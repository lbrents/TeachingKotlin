# Lambda Functions

This is probably the most complicated part of this guide, so honestly it's definitely fine if this doesn't make sense at first. The key takeaway is going to be recognizing **when** you need a lambda function, not the complicated stuff about what exactly they are.

So after that preface, what exactly is a lambda function? A lambda function is essentially a function that can be treated like a variable. But how can a function be put into a variable when it doesn't even have a data type? Well... it kinda has a data type, so lets look at an example once again.

``` kotlin
```
